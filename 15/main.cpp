#include"Picture.hpp"
#include<iostream>
#include<vector>
#include<string>
using namespace std;

int main(){
	vector<string> pic;
	pic = {"this", "is", "picture"};
//	Picture p = pic;
//	Picture q = frame(p);
//	Picture r = hcat(p,q);
//	Picture s = vcat(q,r);

//	cout<< frame(hcat(s, vcat(r,q))) << endl;

//	cout << frame(pic) << endl;
	return 0;
}
